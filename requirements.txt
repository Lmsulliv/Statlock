# Runtime dependencies (API + worker). Pinned to the versions in use.
# The worker uses only the stdlib for HTTP (ingest/client.py: urllib), so these
# two cover both the FastAPI app and `python -m ingest`. pydantic comes in
# transitively via fastapi. Dev-only tools live in requirements-dev.txt.
fastapi==0.136.3
# [standard] pulls the production serving extras (uvloop on Linux, httptools,
# websockets, watchfiles) used when running behind a single origin.
uvicorn[standard]==0.49.0
